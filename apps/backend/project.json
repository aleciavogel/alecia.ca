{
  "name": "backend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/backend",
  "projectType": "application",
  "tags": ["scope:backend", "type:app"],
  "// targets": "to see all targets run: nx show project backend --web",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "doppler run --command='cd apps/backend && next dev --experimental-https --port 3001 --hostname sanity.alecia.local'"
      }
    },
    "build": {
      "executor": "@nx/next:build",
      "options": {
        "postcssConfig": "apps/backend/postcss.config.js"
      }
    }
  }
}
