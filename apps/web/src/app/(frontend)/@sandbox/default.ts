/**
 * Default modal view for when the layout state is unknown
 * @returns null to render nothing unless a link is clicked (prevents 404 errors)
 */
const DefaultSandboxModal = () => null

export default DefaultSandboxModal
