/**
 * Parallel route for when the page /experiments is visited
 * I only want the preview modal to render when a link is clicked
 */
const DefaultExperimentsPagePreviewSlot = () => null

export default DefaultExperimentsPagePreviewSlot
