import { codeSnippetsDataAccess } from './code-snippets-data-access'

describe('codeSnippetsDataAccess', () => {
  it('should work', () => {
    expect(codeSnippetsDataAccess()).toEqual('code-snippets-data-access')
  })
})
