import { projectsDataAccess } from './projects-data-access'

describe('projectsDataAccess', () => {
  it('should work', () => {
    expect(projectsDataAccess()).toEqual('projects-data-access')
  })
})
