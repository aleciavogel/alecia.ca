# alecia.ca

This is a [Next.js](https://nextjs.org/) project bootstrapped with [`create-next-app`](https://github.com/vercel/next.js/tree/canary/packages/create-next-app).

## Getting Started

First, run `cp .env.dist .env.local` to enable [Font Awesome](https://fontawesome.com) licensed icons. 

Once that's done, you can run the development server:

```bash
bun install && bun dev
# or
npm install && npm run dev
# or
yarn install && yarn dev
# or
pnpm install && pnpm dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## To Do

* [] Style `ul` and `ol` components for blog posts and tutorials
* [] Make tutorials mobile friendly
* [] Tutorial navigation checkmarks
* [] Chapter quiz hover effects & confetti
* [] Transition from remark to [rehype](https://github.com/mapbox/rehype-prism)
